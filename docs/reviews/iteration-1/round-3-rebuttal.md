# Iteration 1 / Round 3 / Author

接受两项剩余问题。迭代 2 只用 synthetic trace 检查 predictor/calibration/planner 的不变量，并继续输出 `evidence=synthetic_no_gpu`。真实 route tracer 和 physical slot loader保留在 pending data plane，不会因 simulator 测试通过而改为完成。
