# Iteration 2 / Round 3 / Author

接受。迭代 3 将把 planner 接入 controller/API/UI，但不会自动修改 vLLM 权重；status 必须显示 `control_plane_projection`、`data_plane_ready=false`，评委回放显示 `HISTORICAL REPLAY / SIMULATED EER`。真实 tracer、rolling drift 和 simple-threshold 对照继续列为 pending GPU 实验。
